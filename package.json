{
	"name": "good-svg-background",
	"version": "0.1.0",
	"description": "SVG Background block for Gutenberg block-editor.",
	"author": "Hollands Spoor",
	"license": "GPL-2.0-or-later",
	"main": "block/index.js",
	"scripts": {
		"build": "wp-scripts build --webpack-copy-php --output-path=block",
		"format": "wp-scripts format",
		"lint:css": "wp-scripts lint-style",
		"lint:js": "wp-scripts lint-js",
		"packages-update": "wp-scripts packages-update",
		"plugin-zip": "wp-scripts plugin-zip",
		"start": "wp-scripts start --webpack-copy-php --output-path=block",
		"deploy": "npm run build && deploy.bat"
	},
	"devDependencies": {
		"@wordpress/scripts": "^27.9.0"
	}
}
